# .golangci.yml
run:
  timeout: 5m
  go: "1.24"

linters:
  disable-all: true
  errcheck:
    ignore: '.*_test\.go|.*/test/.*'
  enable:
    - gofmt
    - govet
    - staticcheck
    - gosimple

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

output:
  formats: colored-line-number
